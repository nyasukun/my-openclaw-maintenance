{
  "id": "agent-command",
  "name": "Agent Command",
  "description": "List configured purpose agents, run explicit /agent requests, and switch channel sessions with /session.",
  "version": "0.1.0",
  "activation": {
    "onStartup": true,
    "onCommands": [
      "agent",
      "session"
    ]
  },
  "commandAliases": [
    {
      "name": "agent",
      "kind": "runtime-slash"
    },
    {
      "name": "session",
      "kind": "runtime-slash"
    }
  ],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
